services:
  oddjob:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/its-the-vibe/oddjob:latest
    pull_policy: always
    read_only: true
    restart: on-failure:10
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    volumes:
      - ./config.json:/config.json:ro
    command: ["-config", "/config.json"]
